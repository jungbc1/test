const select_pay = function(tag){
	console.log(tag);
	// let periods = document.getElementsByClassName("on")
    // for (const period of periods) {
    //     period.classList.remove("active");
    // }
    // tag.classList.add("active");
	// preventClick(event);
}

// onclick달린 a태그의 이동막기.
function preventClick(e){
	e.preventDefault()
	// log(e.target)
}